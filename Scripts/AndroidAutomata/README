sudo pip install geoip2
sudo apt-get install sqlite, sshpass
sudo pip install requesocks
GeoIP DB:
http://dev.maxmind.com/geoip/geoip2/geolite2/

sudo apt-get install polipo


ProxyDroid Setup
Setchrome is default
Point to sock
Change bypass address:
	127.0.0.1